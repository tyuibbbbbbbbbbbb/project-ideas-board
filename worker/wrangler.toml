name = "project-ideas-worker"
main = "src/index.js"
compatibility_date = "2024-11-01"

# משתנים ציבוריים (לא סודיים) - מגדירים איפה שמור מסד הנתונים
[vars]
DATA_OWNER = "tyuibbbbbbbbbbbb"
DATA_REPO = "project-ideas-data"
DATA_BRANCH = "master"
# רשימת כתובות מותרות לגשת ל-API (האתר שלך ב-GitHub Pages)
ALLOWED_ORIGIN = "https://tyuibbbbbbbbbbbb.github.io"

# סודות (מוגדרים בנפרד באמצעות `wrangler secret put`, לא נשמרים כאן):
#   GITHUB_TOKEN  - Personal Access Token עם הרשאת כתיבה לריפו project-ideas-data בלבד
#   JWT_SECRET    - מחרוזת אקראית ארוכה לחתימת טוקני התחברות
